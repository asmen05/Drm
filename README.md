# IPTV WebSocket Stream Reader
# قارئ بث IPTV عبر WebSocket

أداة بسيطة لقراءة وحفظ البيانات من بث IPTV عبر WebSocket.

## المتطلبات

- Python 3.6 أو أحدث
- مكتبة websocket-client

## التثبيت

```bash
pip install -r requirements.txt
```

أو:

```bash
pip install websocket-client
```

## الاستخدام

### 1. قراءة البيانات فقط (بدون حفظ)

```bash
python websocket_reader.py
```

أو مع رابط مخصص:

```bash
python websocket_reader.py wss://103.237.103.54:60000/live?oid=2lanhaif80b44fbzbuxhpg495m7znkv4
```

### 2. قراءة وحفظ البيانات في ملف

```bash
python websocket_reader.py -o stream.ts
```

أو:

```bash
python websocket_reader.py wss://YOUR_URL_HERE -o output.ts
```

### 3. قراءة مع عرض معلومات مفصلة

```bash
python websocket_reader.py -v
```

### 4. قراءة وحفظ مع معلومات مفصلة

```bash
python websocket_reader.py -o stream.ts -v
```

## الخيارات المتاحة

| الخيار | الوصف |
|--------|-------|
| `url` | رابط WebSocket (اختياري، يوجد رابط افتراضي) |
| `-o, --output` | ملف الإخراج لحفظ البيانات المستقبلة |
| `-v, --verbose` | عرض معلومات مفصلة عن البيانات المستقبلة |
| `-h, --help` | عرض رسالة المساعدة |

## أمثلة

### مثال 1: قراءة بسيطة
```bash
python websocket_reader.py
```

الناتج:
```
🔄 جاري الاتصال بـ: wss://103.237.103.54:60000/live?oid=2lanhaif80b44fbzbuxhpg495m7znkv4
✅ تم الاتصال بنجاح بـ: wss://103.237.103.54:60000/live?oid=2lanhaif80b44fbzbuxhpg495m7znkv4
```

### مثال 2: حفظ البث في ملف
```bash
python websocket_reader.py -o my_stream.ts
```

الناتج:
```
🔄 جاري الاتصال بـ: wss://103.237.103.54:60000/live?oid=2lanhaif80b44fbzbuxhpg495m7znkv4
✅ تم الاتصال بنجاح بـ: wss://103.237.103.54:60000/live?oid=2lanhaif80b44fbzbuxhpg495m7znkv4
💾 سيتم حفظ البيانات في: my_stream.ts
```

### مثال 3: مع معلومات مفصلة
```bash
python websocket_reader.py -o stream.ts -v
```

الناتج:
```
🔄 جاري الاتصال بـ: wss://103.237.103.54:60000/live?oid=2lanhaif80b44fbzbuxhpg495m7znkv4
✅ تم الاتصال بنجاح بـ: wss://103.237.103.54:60000/live?oid=2lanhaif80b44fbzbuxhpg495m7znkv4
💾 سيتم حفظ البيانات في: stream.ts
[2025-11-04 10:30:15] تم استقبال: 8192 بايت | الإجمالي: 8192 بايت
[2025-11-04 10:30:16] تم استقبال: 8192 بايت | الإجمالي: 16384 بايت
...
```

## إيقاف البرنامج

لإيقاف البرنامج، اضغط `Ctrl+C`

## ملاحظات

- البيانات المستقبلة عادة ما تكون بصيغة MPEG-TS (Transport Stream)
- يمكنك تشغيل الملف المحفوظ باستخدام مشغلات الفيديو مثل VLC:
  ```bash
  vlc stream.ts
  ```
- تأكد من أن لديك اتصال إنترنت مستقر
- قد تحتاج إلى صلاحيات للوصول إلى الخادم

## استكشاف الأخطاء

### خطأ في الاتصال
إذا ظهر خطأ في الاتصال، تحقق من:
- صحة الرابط
- اتصالك بالإنترنت
- أن الخادم يعمل ومتاح

### خطأ في التثبيت
إذا واجهت مشكلة في تثبيت المكتبات:
```bash
pip install --upgrade pip
pip install websocket-client
```

## الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.
